<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Noticia</title>
    <link rel="icon" href="/images/logo.png" type="image/x-icon">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h2>Editar Noticia</h2>
        <form action="/admin/noticias/update" method="POST">
            <input type="hidden" name="id" value="<%= noticiaedit.id %>">
            <div class="mb-3">
                <label for="titulo" class="form-label">Título</label>
                <input type="text" class="form-control" id="titulo" name="titulo" value="<%= noticiaedit.titulo %>" required>
            </div>
            <div class="mb-3">
                <label for="descripcion" class="form-label">Descripción</label>
                <textarea class="form-control" id="descripcion" name="descripcion" rows="4" required><%= noticiaedit.descripcion %></textarea>
            </div>
            <div class="mb-3">
                <label for="url_imagen" class="form-label">URL Imagen</label>
                <input type="text" class="form-control" id="url_imagen" name="url_imagen" value="<%= noticiaedit.url_imagen %>" required>
            </div>
            <div class="mb-3">
                <label for="url_mas_info" class="form-label">URL Más Información</label>
                <input type="text" class="form-control" id="url_mas_info" name="url_mas_info" value="<%= noticiaedit.url_mas_info %>" required>
            </div>
            <button type="submit" class="btn btn-primary">Actualizar Noticia</button>
        </form>
    </div>
    <script>
        function checkAuthentication() {
            const userCorreo = localStorage.getItem('userCorreo');
            if (!userCorreo) {
                window.location.href = '/admin/login';
            }
        }
    
        document.addEventListener('DOMContentLoaded', checkAuthentication);
    </script>
</body>
</html>
